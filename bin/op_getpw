#!/bin/sh

# Use the 1Password command to get a password.
if [ $# -ne 1 ]; then
	printf "Usage: %s <item-name>\n" "$0" >&2
	exit
fi

op get item "$1" \
	| jq '.details.fields[] | select(.designation=="password").value'
