#!/bin/sh
#
# Activate an external monitor and switch off the laptop screen.

check_displayport_connected() {
	xrandr --query | grep "^DP1 connected"
}

activate_displayport() {
	xrandr --output LVDS1 --off
	xrandr --output DP1 --auto
}

check_displayport_connected && activate_displayport
