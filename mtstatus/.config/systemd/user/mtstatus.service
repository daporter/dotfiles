[Unit]
Description=X11 status bar
After=graphical-session.target

[Service]
ExecStart=/usr/local/bin/mtstatus
ExecReload=/usr/bin/kill -SIGUSR1 $MAINPID
Restart=always

[Install]
WantedBy=graphical-session.target

